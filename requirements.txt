# ============================================================
# Overtime 项目依赖清单（按实际 import 整理）
# 安装：pip install -r requirements.txt
# 建议使用 Python 3.9+（LangChain 1.x 最低要求）
# ============================================================

# ---- 核心依赖（所有模块共用）----
openpyxl>=3.0.5
requests>=2.24.0
lxml>=4.6.1

# ---- Windows Excel COM 自动化（仅 Windows；demos.py / process_attendance.py 使用 win32com）----
pywin32>=300

# ---- AI 总结（summary.py）----
# langchain 主包提供 langchain.agents.create_agent
langchain>=1.0.0
langchain-core>=1.2.7
langchain-openai>=0.3.0
