"""
全局年月配置（手动修改的唯一入口）。

本项目的加班计算与串休工具在启动时统一读取这里的 YEAR / MONTH，
不再各自用 datetime.now() 推算“上个月”或写死年份。

使用方法：
- 直接修改下方 YEAR / MONTH 为你想要处理的目标年、月；
- 例如要计算 2026 年 9 月的加班数据，设为 YEAR=2026, MONTH=9 即可；
- MONTH 取 1~12 的整数，无需补零（代码内部会自行格式化）。
"""

# 目标年份（四位数）
YEAR = 2026

# 目标月份（1~12）
MONTH = 8

# ========== 新增：夜班人员年假配置 ==========
# 格式：{工号：年假天数}
# 默认 0 天（不在字典中的员工不扣减）
# 示例：
# NIGHT_EMPLOYEE_VACATION_DAYS = {
#     "Q4642": 2,  # 工号 Q4642 休了 2 天年假
#     "Q4643": 1,  # 工号 Q4643 休了 1 天年假
# }
# ===========================================
NIGHT_EMPLOYEE_VACATION_DAYS = {
	"60836": 4,
}

# 最低工资（用于计算加班费基数）
# 若 basic_salary 小于此值，则使用此最低工资作为基数
# 单位：元，支持小数，例如 3000.0
MIN_WAGE = 2230
